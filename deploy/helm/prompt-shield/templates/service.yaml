apiVersion: v1
kind: Service
metadata:
  name: {{ include "prompt-shield.fullname" . }}
  labels:
    {{- include "prompt-shield.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prompt-shield.selectorLabels" . | nindent 4 }}
